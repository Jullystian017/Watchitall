specVersion: "v1"
serviceId: "watchitall"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "watchitall-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
